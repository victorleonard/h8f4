{
  "name": "h8f4",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync-assets": "node scripts/sync-live-assets.mjs",
    "generate-og-image": "node scripts/generate-og-image.mjs",
    "optimize-static-photos": "node scripts/optimize-static-photos.mjs",
    "dev": "npm run sync-assets && npm run optimize-static-photos && npm run generate-og-image && astro dev",
    "build": "npm run sync-assets && npm run optimize-static-photos && npm run generate-og-image && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.0",
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.7.10",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "sharp": "^0.35.0"
  }
}
